# Secret key for JWT signing (use a strong, random value in production)
JWT_SECRET=your_jwt_secret_here

# Slack webhook for sending birthday notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# Initial admin user setup
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123
ADMIN_DOB=YYYY-MM-DD

# Redis connection string (host: redis matches docker-compose service name)
REDIS_URL=redis://redis:6379/0

# Postgres DB URL (host: postgres matches docker-compose service name)
DATABASE_URL=postgresql://buddy:password@postgres:5432/birthdaydb
